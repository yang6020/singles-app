const styles = theme => ({
  root: {
    height: "10vh",
    position: "absolute",
    width: "100vw",
    bottom: 0,
    justifyContent: "space-evenly"
  },

  icon: {
    margin: "2rem"
  }
});

export default styles;
